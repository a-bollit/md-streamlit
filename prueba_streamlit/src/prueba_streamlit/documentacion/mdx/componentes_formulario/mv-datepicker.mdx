Documentación mv-datepicker
link Información
link Ejemplo de uso
Propiedades:

link adapt
link appearance
link customIcon
link dateClass
link datepicker
link datepickerFilter
link delay
link disabled
link disabledDatepickerToggle
link disabledDatepickerInput
link hint
link hintAlign
link hintType
link label
link max
link min
link placeholder
link required
link startAt
link startView
link touchUi
link validateFirst
link validateOnType
Eventos:

link changeEvent
link keyupEvent
Información
Información del componente mv-datepicker.
code
El componente mv-datepicker se basa en los componentes mat-input y mat-datepicker de Angular Material, a los que se les añade funcionalidad y estilos adicionales.

Sirve para poder escoger una fecha en el calendario que se despliega.

Ejemplo de uso
Uso básico del componente mv-datepicker.
code
A continuación se muestra un ejemplo básico de uso del componente.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      [(ngModel)]="datepicker001Model">
    </mv-input>
    
Datepicker básico:

Fecha
Valor variable 'datepicker001Model':

adapt
Ancho del 100%.
code
La propiedad adapt permite definir el ancho del componente al 100% del contenedor.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      adapt=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      adapt=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true>
    </mv-input>
    
Con adapt a true:

Fecha
Con adapt a false:

Fecha
Sin adapt (comportamiento por defecto):

Fecha
appearance
Apariencia del input.
code
La propiedad appearance permite definir la apariencia del componente, por defecto outline.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      appearance="legacy">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      appearance="standard">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      appearance="fill">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      appearance="outline">
    </mv-input>
    
Con apariencia 'legacy':

Fecha
Con apariencia 'standard':

Fecha
Con apariencia 'fill':

Fecha
Con apariencia 'outline':

Fecha
customIcon
Icono del botón.
code
La propiedad customIcon permite definir el icono del botón de abrir el calendario. Los iconos son los de Material https://material.io/resources/icons .

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      customIcon="favorite">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      customIcon="flight_takeoff">
    </mv-input>
    
Icono 'favorite':

Fecha
Icono 'flight_takeoff':

Fecha
dateClass
CSS aplicable a fechas.
code
La propiedad dateClass permite aplicar una clase CSS a unas fechas determinadas.

HTML
TS
CSS

    <mv-input
      label="Fecha"
      datepicker=true
      [dateClass]="customDateClass">
    </mv-input>
    
Con una clase personalizada para resaltar en naranja los días 1 y 20 de cada mes:

Fecha
datepicker
Establecer un input como datepicker.
code
La propiedad datepicker permite definir un mv-input como datepicker.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true>
    </mv-input>
    
Con datepicker = true:

Fecha
datepickerFilter
Filtro a aplicar.
code
La propiedad datepickerFilter permite aplicar un filtro a las fechas.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      [datepickerFilter]="myCustomFilter">
    </mv-input>
    
Filtro para poder elegir solamente lunes, miércoles y viernes:

Fecha
delay
Retardo del componente.
code
La propiedad delay permite definir un retardo después de escribir en el componente, por defecto su valor es 0ms.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      (changeEvent)="changeEventFunctionMillis($event)"
      delay="3000">
    </mv-input>
    
Con delay de 3000 ms:

Fecha
disabled
Deshabilitar el input.
code
La propiedad disabled permite deshabilitar el componente.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      disabled=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      disabled=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true>
    </mv-input>
    
Con disabled a true:

Fecha
Con disabled a false:

Fecha
Sin disabled (comportamiento por defecto):

Fecha
disabledDatepickerToggle
Deshabilitar el botón de abrir el calendario.
code
La propiedad disabledDatepickerToggle permite deshabilitar el botón de abrir el calendario.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      disabledDatepickerToggle=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      disabledDatepickerToggle=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true>
    </mv-input>
    
Con disabledDatepickerToggle a true:

Fecha
Con disabledDatepickerToggle a false:

Fecha
Sin disabledDatepickerToggle (comportamiento por defecto):

Fecha
disabledDatepickerInput
Deshabilitar el input.
code
La propiedad disabledDatepickerInput permite deshabilitar el input del datepicker.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      disabledDatepickerInput=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      disabledDatepickerInput=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true>
    </mv-input>
    
Con disabledDatepickerInput a true:

Fecha
Con disabledDatepickerInput a false:

Fecha
Sin disabledDatepickerInput (comportamiento por defecto):

Fecha
hint
Texto explicativo.
code
La propiedad hint permite definir un texto debajo del componente.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      hint="Propiedad hint">
    </mv-input>
    
Con hint 'Propiedad hint':

Fecha
Propiedad hint
hintAlign
Posición del hint.
code
La propiedad hintAlign permite establecer la posición del hint al principio o al final del componente.

En caso de usar esta propiedad también tendrá que estar definida la propiedad hint

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      hintAlign="start"
      hint="Hint al principio">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      hintAlign="end"
      hint="Hint al final">
    </mv-input>
    
Con hintAlign 'start':

Fecha
Hint al principio
Con hintAlign 'end':

Fecha
Hint al final
hintType
Estilo del hint.
code
La propiedad hintType permite definir el estilo del hint entre los valores warning, info, danger y success.

En caso de usar esta propiedad también tendrá que estar definida la propiedad hint

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      hintType="info"
      hint="Hint info">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      hintType="success"
      hint="Hint success">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      hintType="warning"
      hint="Hint warning">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      hintType="danger"
      hint="Hint danger">
    </mv-input>
    
Con hintType 'info':

Fecha
Hint info
Con hintType 'success':

Fecha
Hint success
Con hintType 'warning':

Fecha
Hint warning
Con hintType 'danger':

Fecha
Hint danger
label
Label del input.
code
La propiedad label permite definir un texto que queda por encima del componente, a modo de título.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      label="Label del input"
      appearance="legacy">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      label="Label del input"
      appearance="standard">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      label="Label del input"
      appearance="fill">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      label="Label del input"
      appearance="outline">
    </mv-input>
    
Con label 'Label del input' y apariencia 'legacy':

Label del input
Con label 'Label del input' y apariencia 'standard':

Label del input
Con label 'Label del input' y apariencia 'fill':

Label del input
Con label 'Label del input' y apariencia 'outline':

Label del input
max
Fecha máxima.
code
La propiedad max permite definir la fecha máxima seleccionable.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      [max]="maxDate">
    </mv-input>
    
Con max a 31 de agosto del año actual:

Fecha
min
Fecha mínima.
code
La propiedad min permite definir la fecha mínima seleccionable.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      [min]="minDate">
    </mv-input>
    
Con min a 15 de julio del año actual:

Fecha
placeholder
Placeholder del input.
code
La propiedad placeholder permite definir un texto que queda por encima del componente, a modo de título.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      placeholder="Fecha inicio">
    </mv-input>
    
Con placeholder 'Fecha inicio':

Fecha
required
Datepicker obligatorio.
code
La propiedad required permite definir el input como obligatorio.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      required=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      required=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true>
    </mv-input>
    
Con required a true:

Fecha *
Con required a false:

Fecha
Sin required (comportamiento por defecto):

Fecha
requiredHint
Mensaje del input required.
code
La propiedad requiredHint permite definir el mensaje cuando el input es required.

En caso de usar esta propiedad también tendrá que estar definida la propiedad required

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      requiredHint="Campo obligatorio"
      required=true>
    </mv-input>
    
Con requiredHint 'Campo obligatorio':

Fecha *
startAt
Fecha de inicio.
code
La propiedad startAt permite definir la fecha en la que empieza el datepicker.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      [startAt]="startAtDate">
    </mv-input>
    
Con startAt a 1 de septiembre del año actual:

Fecha
startView
Vista de día/mes/año.
code
La propiedad startView permite definir qué queremos ver cuando se abre el calendario: los días del mes, los meses del año o los años.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      startView="month">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      startView="year">
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      startView="multi-year">
    </mv-input>
    
Con startView 'month':

Fecha
Con startView 'year':

Fecha
Con startView 'multi-year':

Fecha
touchUi
Mostrar el datepicker como vista touch.
code
La propiedad touchUi permite mostrar el datepicker con vista más amigable y grande si el tamaño de la pantalla lo permite.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      touchUi=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      touchUi=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true>
    </mv-input>
    
Con touchUi a true:

Fecha
Con touchUi a false:

Fecha
Sin touchUi (comportamiento por defecto):

Fecha
validateFirst
Validación inicial del componente.
code
La propiedad validateFirst permite validar o no el componente al ser cargado, ya que a lo mejor no queremos que de primeras se marquen como erróneos determinados datepicker o al revés, queremos que nada más cargar la vista se haga la validación

En caso de usar esta propiedad también tendrá que estar definida alguna propiedad que se pueda validar, como por ejemplo required=true

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      required=true
      validateFirst=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      required=true
      validateFirst=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      required=true>
    </mv-input>
    
Con validateFirst a true:

Fecha *
Campo requerido
Con validateFirst a false:

Fecha *
Sin validateFirst (comportamiento por defecto):

Fecha *
validateOnType
Validación en tiempo real.
code
La propiedad validateOnType permite activar la validación del componente en tiempo real, esto es según se va escribiendo.

En caso de usar esta propiedad también tendrá que estar definida alguna propiedad que se pueda validar, como por ejemplo required=true

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      required=true
      validateOnType=true>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      required=true
      validateOnType=false>
    </mv-input>

    <mv-input
      label="Fecha"
      datepicker=true
      required=true>
    </mv-input>
    
Con validateOnType a true:

Fecha *
Con validateOnType a false:

Fecha *
Sin validateOnType (comportamiento por defecto):

Fecha *
changeEvent
Evento change.
code
El evento changeEvent permite definir una función que se ejecutará cuando cambie el valor del input

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      (changeEvent)="changeEventFunction($event)">
    </mv-input>
    
Con changeEvent:

Fecha
keyupEvent
Evento keyup.
code
El evento keyupEvent permite definir una función que se ejecutará cuando el usuario deja de teclear

La diferencia con el evento changeEvent es que no se ejecutará la función keyupEvent hasta que el usuario deje de teclear, de manera que si mantiene pulsado de manera continuada un valor del teclado se estará introduciendo muchos caracteres sin que se ejecute la función keyupEvent, sin embargo sí se estaría ejecutando la función changeEvent porque el valor sí está cambiando.

HTML
TS

    <mv-input
      label="Fecha"
      datepicker=true
      (keyupEvent)="keyupEventFunction($event)">
    </mv-input>
    
Con keyupEvent:

Fecha